#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while(t--){
    	long int n,i;
    	cin >> n;
    	long int a[n];
    	for (int i = 0; i < n; ++i)
    	{
    		cin >> a[i];
    	}
    	
    }
}